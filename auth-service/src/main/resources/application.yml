server:
  port: 8081

spring:
  application:
    name: auth-service
  r2dbc:
    url: r2dbc:postgresql://${DB_HOST:localhost}:5432/auth_db
    username: ${DB_USER:postgres}
    password: ${DB_PASSWORD:postgres}
  sql:
    init:
      mode: always
      schema-locations: classpath:db/schema.sql

security:
  jwt:
    secret: ${JWT_SECRET:32-character-long-secret-key-for-jwt-generation-and-validation}
    expiration: 3600000
    refresh-expiration: 86400000
    internal-token: ${INTERNAL_TOKEN:my-super-secret-internal-token}

google:
  client-id: ${GOOGLE_CLIENT_ID:}

catalog:
  service:
    url: ${CATALOG_SERVICE_URL:http://catalog-service:8082}

eureka:
  client:
    service-url:
      defaultZone: http://${EUREKA_HOST:localhost}:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
